/**
 * Update this before each event. It segregates discussion questions per event —
 * changing the name here (and redeploying) starts a fresh discussion board without
 * touching data from past events.
 */
export const CURRENT_EVENT_NAME: string = "meetup-8";
